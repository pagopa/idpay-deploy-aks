microservice-chart:
  image:
    repository: ghcr.io/pagopa/idpay-timeline
    tag: v1.5.1@sha256:2f2384110c3f2399f2d4853538205c78ec9e01cfa6aa16c0b8fc87607e88490a
    pullPolicy: Always

  ingress:
    host: idpay.itn.internal.cstar.pagopa.it

  resources:
    requests:
      memory: "1Gi"
      cpu: "400m"
    limits:
      memory: "1536Mi"
      cpu: "800m"

  deployment:
    replicas: 3 # This value must be set equal to autoscaling.minReplica

  autoscaling:
    enable: true
    minReplica: 3
    maxReplica: 6
    pollingInterval: 30 # seconds
    cooldownPeriod: 300 # seconds
    triggers:
      - type: cpu
        metadata:
          type: Utilization # Allowed types are 'Utilization' or 'AverageValue'
          value: "60"
      - type: memory
        metadata:
          type: Utilization
          value: "90"

  envSecret:
    APPLICATIONINSIGHTS_CONNECTION_STRING: appinsights-instrumentation-key

  keyvault:
    name: "cstar-p-itn-idpay-kv"
    tenantId: "7788edaf-0346-4068-9d79-c868aed15b3d"


  postman-test:
    run: true
    repoName: idpay-timeline
    dir: postman
    collectionName: "idpay-timeline-e2e.postman_collection.json"
    envVariablesFile: "idpay.postman_environment.json"
